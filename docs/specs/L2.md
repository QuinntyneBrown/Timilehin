# L2 — Detailed Requirements

---

## L1-1: Responsive Layout System

### L2-1.1: Mobile Layout (375px)
**Traces to:** L1-1
Single-column layout with hamburger menu navigation, stacked content sections, and touch-friendly tap targets (min 44px).
**Acceptance Criteria:**
- Mobile frame is 375px wide
- Navigation collapses to hamburger icon with logo
- All content sections stack vertically
- Text is legible without zooming (min 16px body text)

### L2-1.2: Tablet Layout (768px)
**Traces to:** L1-1
Two-column grid where appropriate, with visible navigation links and balanced spacing.
**Acceptance Criteria:**
- Tablet frame is 768px wide
- Navigation shows key links inline
- Content uses 2-column grid where suitable
- Proper padding and margins for tablet reading

### L2-1.3: Desktop Layout (1440px)
**Traces to:** L1-1
Full-width layout with horizontal navigation bar, multi-column content areas, and sidebar where appropriate.
**Acceptance Criteria:**
- Desktop frame is 1440px wide
- Full horizontal navigation with all links visible
- Hero section spans full width
- Content areas use up to 3 columns where appropriate

---

## L1-2: Verse of the Day Module

### L2-2.1: Verse of the Day Card
**Traces to:** L1-2
A visually distinct card/section displaying the daily verse text, scripture reference, and a decorative or inspirational background/accent.
**Acceptance Criteria:**
- Verse text displayed in prominent, readable typography
- Scripture reference (e.g., "John 3:16") shown below the verse
- Visual distinction from surrounding content (card, background color, or decorative element)
- A share or copy button is visible
- Responsive across all three breakpoints

---

## L1-3: Bible Reader

### L2-3.1: Bible Navigation Interface
**Traces to:** L1-3
A book/chapter selector allowing users to navigate to any Bible book and chapter.
**Acceptance Criteria:**
- Dropdown or list for selecting a Bible book
- Chapter number selector appears after book selection
- Current selection is clearly indicated
- Works on all breakpoints

### L2-3.2: Bible Reading Pane
**Traces to:** L1-3
A clean reading area displaying verse text for the selected chapter with verse numbers, optimized for readability.
**Acceptance Criteria:**
- Verse text displayed with verse numbers
- Clean typography optimized for extended reading (serif or readable sans-serif, adequate line height)
- Scrollable content area for long chapters
- Note referencing free Bible API integration (bible-api.com)

---

## L1-4: Devotionals Section

### L2-4.1: Daily Devotional Card
**Traces to:** L1-4
A devotional entry displaying title, date, scripture reference, reflection body text, and a closing prayer prompt.
**Acceptance Criteria:**
- Devotional title and date displayed prominently
- Scripture reference linked/styled distinctly
- Reflection/body text area with comfortable reading typography
- Prayer prompt section visually separated
- Responsive across all breakpoints

### L2-4.2: Devotional Archive/List
**Traces to:** L1-4
A list or grid of past devotionals that users can browse.
**Acceptance Criteria:**
- Grid or list of devotional entries with title, date, and excerpt
- Visual hierarchy distinguishes today's devotional from past entries
- Pagination or "load more" indicator present

---

## L1-5: Homepage & Navigation

### L2-5.1: Hero Section
**Traces to:** L1-5
A visually engaging hero area at the top of the homepage with a headline, subtitle, and call-to-action button.
**Acceptance Criteria:**
- Large hero image or gradient background
- Headline text (e.g., site name / tagline)
- Call-to-action button (e.g., "Start Reading" or "Today's Devotional")
- Responsive sizing across all breakpoints

### L2-5.2: Navigation Bar
**Traces to:** L1-5
A consistent top navigation bar with links to Home, Bible Reader, Devotionals, and Verse of the Day sections. The navbar is sticky and remains visible at the top of the viewport when the user scrolls.
**Acceptance Criteria:**
- Logo/brand name on the left
- Navigation links: Home, Read Bible, Devotionals, Verse of the Day
- Active state indicator for current page
- Responsive: full nav on desktop/tablet, hamburger on mobile
- Navbar is sticky (position: sticky, top: 0) and stays visible when scrolling
- Navbar has a z-index high enough to stay above page content

### L2-5.3: Homepage Content Sections
**Traces to:** L1-5
The homepage ties together featured sections: Verse of the Day, a Bible Reader teaser, and a Devotional highlight.
**Acceptance Criteria:**
- Verse of the Day section prominently placed
- Bible Reader teaser with a "Read Now" link
- Latest devotional preview with a "Read More" link
- Clear visual separation between sections
- Footer with basic links and copyright

---

## L1-6: Backend API — Verse of the Day

### L2-6.1: Verse of the Day Endpoint
**Traces to:** L1-6
A GET endpoint at `/api/verseoftheday` that returns today's Bible verse reference and text.
**Acceptance Criteria:**
- GET `/api/verseoftheday` returns 200 with JSON containing `reference` and `text` fields
- The verse rotates daily based on a curated list of popular Bible verses
- Response is cached in the database per day to avoid repeat external API calls
- If the external API call fails, the endpoint returns a graceful fallback response (not 500)

### L2-6.2: Verse of the Day Caching
**Traces to:** L1-6
The VerseOfTheDay entity is persisted in SQLite with a unique date index, so each day's verse is fetched from bible-api.com at most once.
**Acceptance Criteria:**
- A `VerseOfTheDay` model exists with `Id`, `Date`, `Reference`, and `Text` properties
- The `Date` column has a unique index
- On first request of the day, the verse is fetched from bible-api.com and stored
- Subsequent requests on the same day return the cached database record

---

## L1-7: Backend API — Bible Reader

### L2-7.1: Bible Chapter Endpoint
**Traces to:** L1-7
A GET endpoint at `/api/bible/{book}/{chapter}` that returns all verses for the given book and chapter.
**Acceptance Criteria:**
- GET `/api/bible/Genesis/1` returns 200 with JSON containing `reference`, `verses` array, and `translation`
- Each verse object contains `verse` (number) and `text` (string)
- Returns 404 with a descriptive message for invalid book/chapter combinations
- The `book` parameter supports multi-word book names (e.g., `1 Corinthians`)

---

## L1-8: Backend API — Devotionals

### L2-8.1: Devotional List Endpoint
**Traces to:** L1-8
A GET endpoint at `/api/devotionals` that returns a paginated list of devotional summaries.
**Acceptance Criteria:**
- GET `/api/devotionals` returns 200 with paginated results
- Supports `page` and `pageSize` query parameters (defaults: page=1, pageSize=10)
- Results are ordered by date descending (newest first)
- Each item includes `id`, `title`, `date`, `scriptureReference`, and a truncated `excerpt` (max 150 chars)
- Response includes `totalCount`, `page`, `pageSize`, `totalPages`, `hasNextPage`, `hasPreviousPage`

### L2-8.2: Devotional Detail Endpoint
**Traces to:** L1-8
A GET endpoint at `/api/devotionals/{id}` that returns the full devotional content.
**Acceptance Criteria:**
- GET `/api/devotionals/1` returns 200 with full devotional including `reflectionText` and `prayerPrompt`
- Returns 404 for non-existent IDs

### L2-8.3: Today's Devotional Endpoint
**Traces to:** L1-8
A GET endpoint at `/api/devotionals/today` that returns the devotional matching today's date.
**Acceptance Criteria:**
- GET `/api/devotionals/today` returns 200 with today's devotional if one exists
- Returns 404 with descriptive message if no devotional exists for today

### L2-8.4: Create Devotional Endpoint
**Traces to:** L1-8
A POST endpoint at `/api/devotionals` that creates a new devotional entry.
**Acceptance Criteria:**
- POST `/api/devotionals` with valid JSON body returns 201 with the created resource
- Response includes a `Location` header pointing to the new resource
- Request body requires `title`, `date`, `scriptureReference`, `reflectionText`, `prayerPrompt`

### L2-8.5: Update Devotional Endpoint
**Traces to:** L1-8
A PUT endpoint at `/api/devotionals/{id}` that partially updates an existing devotional.
**Acceptance Criteria:**
- PUT `/api/devotionals/1` with partial JSON body returns 200 with the updated resource
- Only provided fields are updated; omitted fields remain unchanged
- Returns 404 for non-existent IDs

### L2-8.6: Delete Devotional Endpoint
**Traces to:** L1-8
A DELETE endpoint at `/api/devotionals/{id}` that removes a devotional.
**Acceptance Criteria:**
- DELETE `/api/devotionals/1` returns 204 No Content on success
- Returns 404 for non-existent IDs

---

## L1-9: Backend Infrastructure

### L2-9.1: EF Core DbContext and Database
**Traces to:** L1-9
An `AppDbContext` with DbSets for `Devotional` and `VerseOfTheDay`, configured with SQLite and auto-created on startup.
**Acceptance Criteria:**
- `AppDbContext` contains `DbSet<Devotional>` and `DbSet<VerseOfTheDay>`
- Database is created automatically via `EnsureCreatedAsync` on application startup
- Connection string is configured in `appsettings.json`

### L2-9.2: Service Layer Abstractions
**Traces to:** L1-9
All business logic is encapsulated behind `IBibleService` and `IDevotionalService` interfaces, registered via dependency injection.
**Acceptance Criteria:**
- `IBibleService` declares `GetVerseOfTheDayAsync` and `GetChapterAsync` methods
- `IDevotionalService` declares `GetAllAsync`, `GetByIdAsync`, `GetTodayAsync`, `CreateAsync`, `UpdateAsync`, `DeleteAsync` methods
- Services are registered in DI container with appropriate lifetimes
- `BibleApiService` uses `HttpClient` via `IHttpClientFactory` pattern

### L2-9.3: CORS Configuration
**Traces to:** L1-9
The API is configured with a CORS policy that allows the frontend origins.
**Acceptance Criteria:**
- CORS allows origins configured in `appsettings.json` under `Cors:Origins`
- Defaults to `http://localhost:3000` if no configuration is provided
- Allows any header and any method

### L2-9.4: OpenAPI Documentation
**Traces to:** L1-9
The API exposes an OpenAPI specification in development mode.
**Acceptance Criteria:**
- OpenAPI endpoint is mapped when running in Development environment
- All API endpoints are discoverable via the OpenAPI spec

---

## L1-10: Domain Smart Components

### L2-10.1: Verse of the Day Container
**Traces to:** L1-10
A smart component that fetches the verse of the day from `BibleService` and renders it via the `VerseOfTheDayCardComponent`.
**Acceptance Criteria:**
- Component calls `BibleService.getVerseOfTheDay()` on init
- Passes `verseText` and `reference` to `VerseOfTheDayCardComponent`
- Displays a loading indicator while the request is in flight
- Handles errors gracefully with a user-friendly fallback message
- Unit test verifies data flows from service to template

### L2-10.2: Bible Reader Container
**Traces to:** L1-10
A smart component that provides a book selector sidebar and a chapter reading pane. It fetches chapter content from `BibleService` and renders verses via `VerseItemComponent` and navigation via `ChapterNavComponent`.
**Acceptance Criteria:**
- Component holds a list of Bible books (Old Testament and New Testament) with chapter counts
- Selecting a book and chapter calls `BibleService.getChapter(book, chapter)`
- Renders each verse using `VerseItemComponent`
- Renders chapter navigation using `ChapterNavComponent`
- Shows the current book and chapter as a header
- Displays a loading indicator while fetching
- Handles errors gracefully
- Unit test verifies book selection triggers API call and verses render

### L2-10.3: Today Devotional Container
**Traces to:** L1-10
A smart component that fetches today's devotional from `DevotionalService` and renders it via `DevotionalCardComponent`, `ScriptureBlockComponent`, and `PrayerPromptComponent`.
**Acceptance Criteria:**
- Component calls `DevotionalService.getToday()` on init
- Passes title, scriptureReference, reflectionText, and prayerPrompt to `DevotionalCardComponent`
- Displays date and a section label
- Displays a loading indicator while the request is in flight
- Shows a "no devotional for today" message when the API returns 404
- Unit test verifies data flows from service to template

### L2-10.4: Devotional Archive Container
**Traces to:** L1-10
A smart component that fetches a paginated list of past devotionals from `DevotionalService` and renders each entry via `DevotionalSummaryCardComponent`.
**Acceptance Criteria:**
- Component calls `DevotionalService.getAll(page, pageSize)` on init
- Renders each summary with `DevotionalSummaryCardComponent`
- Emits a `devotionalSelected` event with the devotional id when a card is clicked
- Shows "Load More" when `hasNextPage` is true and loads the next page on click
- Displays a loading indicator on initial load
- Unit test verifies pagination triggers and card rendering

### L2-10.5: Homepage Container
**Traces to:** L1-10
A smart component that composes the full homepage by combining the `HeroComponent`, `VerseOfTheDayContainerComponent`, two `FeatureCardComponent`s (Bible Reader teaser and Devotional highlight), and `FooterComponent`.
**Acceptance Criteria:**
- Renders the hero section with correct tagline, title, description, and CTA buttons
- Embeds the verse-of-the-day container
- Renders a Bible Reader feature card and a Devotional feature card
- Renders the footer with brand, copyright, and links
- Hero primary CTA emits a `navigated` event with route `/bible`
- Hero secondary CTA emits a `navigated` event with route `/devotionals`
- Unit test verifies all child components are present in the template

### L2-10.6: Devotional Page Container
**Traces to:** L1-10
A smart component that composes the devotionals page by combining the `TodayDevotionalContainerComponent` and `DevotionalArchiveContainerComponent` in a two-column layout matching the UI design.
**Acceptance Criteria:**
- Renders today's devotional in the main column
- Renders the archive sidebar in a secondary column
- Emits a `devotionalSelected` event when an archive card is clicked
- When a devotional is selected, fetches and displays its detail via `DevotionalCardComponent`
- Unit test verifies both containers are present in the template

---

## L1-11: Frontend App Implementation

### L2-11.1: App Configuration and HTTP Setup
**Traces to:** L1-11
The app must configure `provideHttpClient` and a development proxy so API calls to `/api/*` are forwarded to the .NET backend at `https://localhost:7042`.
**Acceptance Criteria:**
- `app.config.ts` includes `provideHttpClient()`
- A proxy configuration file forwards `/api` requests to the backend
- The Angular dev server uses the proxy config
- App builds without errors

### L2-11.2: Routing Configuration
**Traces to:** L1-11
The app must define routes for Homepage (`/`), Bible Reader (`/bible`), and Devotionals (`/devotionals`).
**Acceptance Criteria:**
- Route `/` renders the homepage
- Route `/bible` renders the Bible reader page
- Route `/devotionals` renders the devotionals page
- Navigation between routes works via the navbar

### L2-11.3: App Shell with Navbar
**Traces to:** L1-11
The root `App` component must render the `NavbarComponent` above a `router-outlet`, with nav links wired to routes.
**Acceptance Criteria:**
- Navbar is visible on all pages with links: Home, Read Bible, Devotionals
- Clicking a nav link navigates to the corresponding route
- The active link is highlighted based on the current route
- Brand name shows "GraceWord" with the book-open icon

### L2-11.4: Homepage Page
**Traces to:** L1-11
A page component at `/` that renders the `HomepageContainerComponent` and handles its `navigated` output to route the user.
**Acceptance Criteria:**
- Homepage renders the hero section, verse of the day, feature cards, and footer
- Clicking "Start Reading" navigates to `/bible`
- Clicking "Explore Devotionals" navigates to `/devotionals`

### L2-11.5: Bible Reader Page
**Traces to:** L1-11
A page component at `/bible` that renders the `BibleReaderContainerComponent`.
**Acceptance Criteria:**
- Bible reader page renders the book sidebar and reading pane
- Selecting a book and chapter loads verse content from the backend API

### L2-11.6: Devotionals Page
**Traces to:** L1-11
A page component at `/devotionals` that renders the `DevotionalPageContainerComponent`.
**Acceptance Criteria:**
- Devotionals page renders today's devotional and the archive sidebar
- Clicking an archive card loads the full devotional detail

### L2-11.7: Global Styles
**Traces to:** L1-11
Global styles must import Google Fonts (Playfair Display, Inter) and set base typography matching the ui-design.pen.
**Acceptance Criteria:**
- Google Fonts loaded for Playfair Display (italic 400, 700) and Inter (400, 500, 600)
- Body uses `$bg-primary` (#FFFAF5) background
- Base font family is Inter
- No default margin/padding on body

---

## L1-12: End-to-End Playwright Tests

### L2-12.1: Playwright Setup and Configuration
**Traces to:** L1-12
Playwright must be installed and configured to run against the Angular dev server proxied to the actual .NET backend.
**Acceptance Criteria:**
- `playwright.config.ts` exists at `src/Timilehin.Web`
- Tests use the dev server base URL
- Configuration supports running with or without a running dev server via `webServer` config

### L2-12.2: Page Object Models
**Traces to:** L1-12
Page objects must be created for each page: HomePage, BibleReaderPage, DevotionalsPage.
**Acceptance Criteria:**
- `HomePage` POM exposes methods for hero actions, verifying verse of the day, navigating via feature cards
- `BibleReaderPage` POM exposes methods for selecting books, navigating chapters, reading verses
- `DevotionalsPage` POM exposes methods for viewing today's devotional and browsing the archive
- All POMs use semantic locators (role, test-id, text)

### L2-12.3: Homepage Flow Tests
**Traces to:** L1-12
E2E tests for homepage user flows.
**Acceptance Criteria:**
- Test verifies the homepage loads with hero section containing title and CTA buttons
- Test verifies verse of the day section is visible with a verse reference
- Test verifies clicking "Start Reading" navigates to `/bible`
- Test verifies clicking "Explore Devotionals" or similar navigates to `/devotionals`

### L2-12.4: Bible Reader Flow Tests
**Traces to:** L1-12
E2E tests for Bible reading user flows using the actual backend API.
**Acceptance Criteria:**
- Test verifies selecting a book (e.g., Genesis) shows the book as active
- Test verifies chapter content loads with verse text from the API
- Test verifies navigating to the next chapter updates the reading pane
- Test verifies the chapter nav shows correct chapter numbers

### L2-12.5: Devotionals Flow Tests
**Traces to:** L1-12
E2E tests for devotional user flows using the actual backend API.
**Acceptance Criteria:**
- Test verifies the devotionals page loads
- Test verifies the archive section is visible
- Test verifies creating a devotional via API and then seeing it in the archive

---

## L1-13: UI Design Verification Tests

### L2-13.1: Typography Verification
**Traces to:** L1-13
Playwright tests verify that the correct fonts and font sizes from ui-design.pen are applied.
**Acceptance Criteria:**
- Hero title uses Playfair Display font family
- Hero title font size is approximately 56px on desktop
- Body text uses Inter font family
- Navigation links use Inter font family at 14px
- Verse of the day text uses Playfair Display italic

### L2-13.2: Spacing and Layout Verification
**Traces to:** L1-13
Playwright tests verify that key spacing values from ui-design.pen are correct.
**Acceptance Criteria:**
- Navbar has padding of 20px vertically and 56px horizontally on desktop
- Hero section has minimum height of 480px on desktop
- Feature cards section uses horizontal layout on desktop
- Footer has dark background (#1A1A2E or similar dark color)
- Navbar has position sticky with top: 0 and remains visible when scrolled

---

## L1-14: Bible Reader Spacing Fix

### L2-14.1: Desktop Bible Reader Spacing (1440px)
**Traces to:** L1-14
The Bible reader sidebar and reading pane must match the ui-design.pen spacing at desktop viewport.
**Acceptance Criteria:**
- Sidebar width is 280px
- Sidebar has vertical padding of 24px and horizontal padding of 0
- Reading pane has vertical padding of 32px and horizontal padding of 64px
- Reading pane has a gap of 32px between child elements
- Reading pane background is $bg-card (#ffffff)
- Sidebar background is $bg-secondary (#f7f0e8)
- Sidebar has a right border of 1px solid $border-light

### L2-14.2: Tablet Bible Reader Spacing (768px)
**Traces to:** L1-14
At tablet viewport the sidebar is hidden, replaced by dropdown selectors in a book bar. The reading pane spacing must match the design.
**Acceptance Criteria:**
- Sidebar is not visible at 768px viewport
- A book/chapter selector bar is visible with padding 12px 24px
- Reading pane has padding of 28px 32px
- Reading pane has a gap of 24px between child elements

### L2-14.3: Mobile Bible Reader Spacing (375px)
**Traces to:** L1-14
At mobile viewport the sidebar is hidden, replaced by dropdown selectors. The reading pane spacing is compact.
**Acceptance Criteria:**
- Sidebar is not visible at 375px viewport
- A book/chapter selector bar is visible with padding 10px 16px
- Reading pane has padding of 20px 16px
- Reading pane has a gap of 16px between child elements

### L2-14.4: Playwright Tests for Bible Reader Spacing
**Traces to:** L1-14
Playwright tests verify the Bible reader spacing at all three breakpoints.
**Acceptance Criteria:**
- Test at 1440px verifies sidebar width, sidebar padding, content padding, and content gap
- Test at 768px verifies sidebar is hidden, book bar padding, content padding, and content gap
- Test at 375px verifies sidebar is hidden, book bar padding, content padding, and content gap
- All tests run and pass

---

## L1-15: Devotionals Page Spacing Fix

### L2-15.1: Desktop Devotionals Spacing (1440px)
**Traces to:** L1-15
The devotionals page must have proper outer padding and grid gap at desktop viewport.
**Acceptance Criteria:**
- Page container has vertical padding of 48px and horizontal padding of 56px
- Two-column grid has a gap of 32px
- Content does not run flush to viewport edges

### L2-15.2: Tablet Devotionals Spacing (768px)
**Traces to:** L1-15
At tablet viewport the devotionals page switches to single-column layout with appropriate padding.
**Acceptance Criteria:**
- Page container has vertical padding of 32px and horizontal padding of 32px
- Single-column layout (grid-template-columns: 1fr)
- Grid gap is 24px

### L2-15.3: Mobile Devotionals Spacing (375px)
**Traces to:** L1-15
At mobile viewport the devotionals page has compact padding.
**Acceptance Criteria:**
- Page container has vertical padding of 24px and horizontal padding of 16px
- Single-column layout
- Grid gap is 16px

### L2-15.4: Playwright Tests for Devotionals Spacing
**Traces to:** L1-15
Playwright tests verify the devotionals page spacing at all three breakpoints.
**Acceptance Criteria:**
- Test at 1440px verifies page padding (48px vertical, 56px horizontal) and grid gap (32px)
- Test at 768px verifies page padding (32px vertical, 32px horizontal) and single-column layout
- Test at 375px verifies page padding (24px vertical, 16px horizontal) and single-column layout
- All tests run and pass
